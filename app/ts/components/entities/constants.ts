

export const INITIAL_VALUE = 100; // must be square of integer

export const LOCAL_WIDTH = 1 / Math.sqrt(INITIAL_VALUE);

export const YELLOW_COLOR = { r: 255, g: 223, b: 0 };

export const MAX_PRESSURE_PER_FRAME = 0.1;
